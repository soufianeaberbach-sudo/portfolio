node_modules/
dist/
.astro/
.wrangler/

# Local Worker secrets
.dev.vars
wrangler.dev.jsonc

# Astro build-time env files. PUBLIC_TURNSTILE_SITEKEY lives here locally, and
# anything else that lands in one must not be committed by accident.
.env
.env.*
!.env.example

# Screenshot output from the Portfolio test suites, and the scratch Playwright
# harnesses used while verifying a change. Neither is source.
.qa-director/
.pfqa/
.pfvis/
