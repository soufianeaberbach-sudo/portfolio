---
import SiteLayout from '../layouts/SiteLayout.astro';

const capabilities = [
  'Sketch to production',
  '2D pattern engineering',
  'Gradation',
  '3D simulation',
  'Production monitoring',
  'Technical tracking',
];

const stages = [
  ['01', 'Sketch', 'The idea is tested against fabric, construction and cost before development begins.'],
  ['02', '2D Pattern', 'Geometry is drafted to the block, tolerances and seam logic the factory will use.'],
  ['03', 'Gradation', 'The full size run is engineered and checked against the measurement chart.'],
  ['04', '3D Simulation', 'Fit, balance and drape are resolved before sampling starts spending time.'],
  ['05', 'Final Garment', 'The factory receives a clear, repeatable production specification.'],
];
---

<SiteLayout
  title="Soufiane Aberbach — Fashion Designer & Product Developer"
  description="Senior fashion designer and product developer in Tangier, focused on production-ready pattern, fit and technical development."
  marker="SA-HOME-V5"
  page="home"
>
  <!-- BUILD-MARKER: SA-HOME-V5 -->
  <section class="home-hero">
    <div class="home-hero__index label">SA / 00—05</div>
    <div class="home-hero__title" data-reveal>
      <p class="eyebrow">Tangier · Morocco</p>
      <h1 class="display"><span>Soufiane</span><span>Aberbach</span></h1>
    </div>
    <div class="home-hero__statement" data-reveal>
      <p class="label">Senior Fashion Designer &amp; Product Developer</p>
      <p>Turning sketches into factory-ready garments through pattern intelligence, 3D simulation and production follow-through.</p>
      <a class="button button--signal" href="/contact/">Send me your sketch <span aria-hidden="true">↗</span></a>
    </div>
    <div class="home-hero__stamp" aria-hidden="true">
      <span>11+ years</span><i></i><span>Production-led</span>
    </div>
  </section>

  <section class="capability-rail" aria-label="Core capabilities">
    <div class="capability-rail__track">
      {[...capabilities, ...capabilities].map((item) => <span>{item}<i aria-hidden="true">◆</i></span>)}
    </div>
  </section>

  <section class="positioning section-shell">
    <div class="positioning__lead" data-reveal>
      <p class="eyebrow">Positioning</p>
      <h2 class="display">The expensive mistake happens <em>before</em> the sewing line.</h2>
    </div>
    <div class="positioning__copy" data-reveal>
      <p>Every extra fitting round, every point of fabric waste and every delayed sample traces back to a decision made before a single metre was cut.</p>
      <p>That decision is the work: defining the pattern, validating fit and translating intent into instructions a factory can repeat.</p>
      <a href="/expertise/" class="text-link">View expertise <span>↗</span></a>
    </div>
  </section>

  <section class="home-process section-shell section-rule">
    <div class="home-process__head" data-reveal>
      <p class="eyebrow">The production sequence</p>
      <h2 class="display">Five stages.<br />No blind spots.</h2>
      <a class="button" href="/process/">Explore the process <span>↗</span></a>
    </div>
    <ol class="home-process__list">
      {stages.map(([index, title, text]) => (
        <li data-reveal>
          <span class="index-tag">{index}</span>
          <h3>{title}</h3>
          <p>{text}</p>
        </li>
      ))}
    </ol>
  </section>

  <section class="brand-context">
    <div class="section-shell" data-reveal>
      <p class="eyebrow">Programme exposure</p>
      <div class="brand-context__names" aria-label="Brand programme exposure">
        <span>Zara</span><span>Bershka</span><span>Oysho</span><span>Massimo Dutti</span><span>Mango</span><span>ASOS</span><span>Adidas</span><span>River Island</span>
      </div>
      <p class="brand-context__note">Project exposure through manufacturing contracts at Ava Marie — not direct employment or endorsement.</p>
    </div>
  </section>

  <section class="home-next section-shell">
    <div data-reveal>
      <p class="label">Selected work / 02</p>
      <h2 class="display">The portfolio moves like the work: layer by layer.</h2>
    </div>
    <a href="/portfolio/" class="home-next__link" data-reveal>
      <span>Open the mechanical portfolio</span><i aria-hidden="true">↗</i>
    </a>
  </section>
</SiteLayout>

<style>
  .home-hero {
    min-height: 100svh;
    position: relative;
    display: grid;
    grid-template-columns: 1.45fr .55fr;
    grid-template-rows: auto 1fr auto;
    gap: 2rem 5vw;
    padding: calc(var(--header-h) + 2.5rem) var(--gutter) 3rem;
    overflow: hidden;
    background:
      linear-gradient(90deg, transparent 0 67%, var(--line) 67% calc(67% + 1px), transparent calc(67% + 1px)),
      var(--paper);
  }
  .home-hero::after {
    content: '';
    position: absolute;
    right: -10vw;
    bottom: -24vw;
    width: min(62vw, 58rem);
    aspect-ratio: 1;
    border: 1px solid var(--line);
    border-radius: 50%;
    box-shadow: inset 0 0 0 5vw var(--paper), inset 0 0 0 calc(5vw + 1px) var(--line);
    pointer-events: none;
  }
  .home-hero__index { grid-column: 1 / -1; align-self: start; color: var(--muted); }
  .home-hero__title { align-self: center; position: relative; z-index: 1; }
  .home-hero__title h1 { font-size: clamp(5rem, 13.4vw, 13rem); }
  .home-hero__title h1 span { display: block; }
  .home-hero__title h1 span:last-child { color: transparent; -webkit-text-stroke: 1.5px var(--ink); }
  .home-hero__statement { align-self: end; position: relative; z-index: 2; padding-bottom: 2rem; }
  .home-hero__statement > p:nth-child(2) { max-width: 30rem; margin: 1.25rem 0 2rem; font-size: clamp(1.1rem, 1.4vw, 1.35rem); line-height: 1.58; }
  .home-hero__stamp {
    position: absolute;
    z-index: 2;
    right: var(--gutter);
    top: 22%;
    width: 9rem;
    aspect-ratio: 1;
    display: grid;
    place-content: center;
    gap: .55rem;
    border: 1px solid var(--line-strong);
    border-radius: 50%;
    text-align: center;
    font-family: var(--mono);
    font-size: .6rem;
    letter-spacing: .12em;
    text-transform: uppercase;
    animation: spin 22s linear infinite;
  }
  .home-hero__stamp i { width: 2rem; height: 1px; margin: auto; background: var(--signal); }
  @keyframes spin { to { transform: rotate(360deg); } }

  .capability-rail { overflow: hidden; border-block: 1px solid var(--line); background: var(--paper-bright); }
  .capability-rail__track { display: flex; width: max-content; animation: rail 28s linear infinite; }
  .capability-rail span { display: inline-flex; align-items: center; gap: 2.5rem; padding: 1.1rem 1.25rem; font-family: var(--mono); font-size: .68rem; letter-spacing: .1em; text-transform: uppercase; white-space: nowrap; }
  .capability-rail i { color: var(--signal); font-size: .45rem; }
  @keyframes rail { to { transform: translateX(-50%); } }

  .positioning { display: grid; grid-template-columns: 1.4fr .6fr; gap: 7vw; padding-block: clamp(7rem, 14vw, 14rem); }
  .positioning h2 { max-width: 68rem; font-size: clamp(4rem, 8vw, 8.5rem); }
  .positioning h2 em { color: var(--signal); font-style: normal; }
  .positioning__copy { align-self: end; max-width: 30rem; }
  .positioning__copy p { color: var(--ink-soft); line-height: 1.75; }
  .text-link { display: inline-flex; gap: 1rem; margin-top: 1.5rem; padding-bottom: .3rem; border-bottom: 1px solid; font-family: var(--mono); font-size: .68rem; letter-spacing: .1em; text-transform: uppercase; }
  .text-link span { color: var(--signal); }

  .home-process { display: grid; grid-template-columns: .7fr 1.3fr; gap: 8vw; padding-block: clamp(6rem, 10vw, 10rem); }
  .home-process__head { position: sticky; top: 8rem; align-self: start; }
  .home-process__head h2 { margin: 2rem 0 3rem; font-size: clamp(4rem, 7vw, 7rem); }
  .home-process__list { margin: 0; padding: 0; list-style: none; border-top: 1px solid var(--line-strong); }
  .home-process__list li { display: grid; grid-template-columns: 3rem 1fr 1.15fr; gap: 1.5rem; align-items: start; padding: 2rem 0; border-bottom: 1px solid var(--line); }
  .home-process__list h3 { margin: .2rem 0 0; font-family: var(--display); font-size: clamp(1.7rem, 3vw, 3rem); line-height: 1; }
  .home-process__list p { margin: 0; color: var(--muted); line-height: 1.65; }

  .brand-context { padding-block: clamp(6rem, 10vw, 10rem); background: var(--ink); color: var(--paper); }
  .brand-context__names { display: grid; grid-template-columns: repeat(4, 1fr); margin-top: 3rem; border-top: 1px solid #45443e; border-left: 1px solid #45443e; }
  .brand-context__names span { padding: 2rem 1.25rem; border-right: 1px solid #45443e; border-bottom: 1px solid #45443e; font-family: var(--display); font-size: clamp(1.4rem, 2.5vw, 2.8rem); font-weight: 700; letter-spacing: -.04em; }
  .brand-context__note { max-width: 45rem; margin: 2rem 0 0; color: #96958d; font-size: .78rem; }

  .home-next { display: grid; grid-template-columns: 1fr .7fr; gap: 8vw; align-items: end; padding-block: clamp(7rem, 12vw, 12rem); }
  .home-next h2 { margin-top: 2rem; font-size: clamp(3.8rem, 7vw, 7.5rem); }
  .home-next__link { display: flex; justify-content: space-between; gap: 2rem; padding: 2rem 0; border-block: 1px solid var(--line-strong); font-family: var(--mono); font-size: .75rem; letter-spacing: .1em; text-transform: uppercase; }
  .home-next__link i { color: var(--signal); font-style: normal; }

  @media (max-width: 900px) {
    .home-hero { grid-template-columns: 1fr; grid-template-rows: auto 1fr auto; background: var(--paper); }
    .home-hero__title { align-self: end; }
    .home-hero__title h1 { font-size: clamp(4.1rem, 18vw, 8rem); }
    .home-hero__statement { max-width: 34rem; }
    .home-hero__stamp { top: 18%; }
    .positioning, .home-process, .home-next { grid-template-columns: 1fr; }
    .positioning__copy { max-width: 40rem; }
    .home-process__head { position: static; }
    .brand-context__names { grid-template-columns: repeat(2, 1fr); }
  }
  @media (max-width: 600px) {
    .home-hero { min-height: 92svh; padding-bottom: 2rem; }
    .home-hero::after, .home-hero__stamp { display: none; }
    .home-hero__title .eyebrow { margin-bottom: 1rem; }
    .home-hero__statement { padding: 0; }
    .home-hero__statement > p:nth-child(2) { font-size: 1rem; }
    .home-process__list li { grid-template-columns: 2.5rem 1fr; }
    .home-process__list p { grid-column: 2; }
    .brand-context__names span { padding: 1.4rem .8rem; font-size: 1.25rem; }
  }
  @media (prefers-reduced-motion: reduce) { .home-hero__stamp, .capability-rail__track { animation: none; } }
</style>
